# Open Abundance Project Memory

Этот документ хранит накопительный контекст проекта: исходную концепцию, продуктовые гипотезы, экономические рассуждения, принятые ограничения, риски и вопросы, которые нельзя терять между итерациями.

Он не является очередью разработки. Общий смысловой канон находится в `OPEN_ABUNDANCE_LORE.md`, конкретные принятые решения — в `OPEN_ABUNDANCE_MASTER_PLAN.md`, а реальный статус и единственный текущий шаг — в `MASTER_KANBAN.md`.

## 1. Исходная идея

Abundance — AI-координируемая система, которая помогает людям превращать желания в действия, развивать навыки, создавать ценность, находить людей и возможности, получать доход и усиливать общий результат.

Главная пользовательская формула:

```text
делаю полезные челленджи -> растет Core -> растет ежедневное начисление
-> появляются новые возможности -> результат становится видимым другим
```

Первый опыт не должен выглядеть как универсальный планировщик, криптокошелек или каталог функций. Пользователь сначала видит результаты, выбирает главное желание, получает финансовый сценарий и выполняет одно действие сегодня.

## 2. Целевая первая аудитория

- люди, ищущие дополнительный или основной доход;
- фрилансеры и создатели контента;
- люди в смене деятельности или переходе к самостоятельной работе;
- пользователи, которым нужен понятный путь к финансовой или жизненной цели.

Первая когорта — 20-50 лично приглашенных русскоязычных пользователей. Это управляемая beta с интервью и ручным наблюдением, а не публичный запуск.

## 3. Главный недельный опыт

```text
история результата
-> главное желание
-> финансовый план
-> Today
-> проверяемая Core-награда
-> рост Core и ежедневного начисления
-> публикация результата
-> возврат завтра
-> приглашение после собственного результата
```

До появления достаточного числа реальных историй допускаются демо-кейсы, но они всегда маркируются как `Демо`. Реальные истории разделяются на `Подтверждено системой` и `Проверено вручную`.

## 4. Экономическая гипотеза

### Core

- Core — неснижаемая внутренняя основа пользователя, определяющая уровень и ежедневное начисление.
- Текущая расчетная ставка — `0.0633%` в день.
- Пользователь может направлять часть ежедневного начисления обратно в Core через reinvest.
- Core не выводится обратно в Wallet и является строго неснижаемым. Исключений для breach, redemption, quality-gate, невыплаты или других событий нет.
- Прямого лимита баланса Core нет.
- Системная выдача Core через челленджи должна быть полезной, проверяемой и защищенной от Sybil abuse, поскольку новый Core увеличивает будущие начисления.
- Текущая защита Core должна отклонять любое уменьшение на уровне базы. Прежний UTC obligations/breach/redemption-контур из `docs/CORE_REDEMPTION_SAFEGUARD_PLAN.md` признан legacy и не может включаться.
- После нескольких ознакомительных челленджей награды предполагают подтверждение уникального человека и реально полезный результат для пользователя или системы.
- 2026-07-30: концептуальные источники Core расширены: Wallet -> Core, проверяемые награды за задачи/челленджи, лидерские и реферальные награды при включенных правилах, другие системные награды и reinvest ежедневного начисления. В beta лидерские и реферальные Wallet-бонусы остаются выключенными до достаточного покрытия.
- 2026-07-30: quality-gate роста должен проверять физическое, ментальное и эмоциональное состояние коротким ежедневным self check-in. Он управляет будущей нагрузкой и ускорением, но не уменьшает уже начисленный Core и не является медицинской диагностикой.
- 2026-07-31: Core подтвержден как абсолютный неснижаемый базовый принцип. Quality-gate, breach, невыплата, ошибка и redemption могут менять только будущий маршрут или операционные статусы, но никогда не баланс Core.
- 2026-07-31: добавлена миграция `20260731120000_core_strictly_non_decreasing.sql`, которая убирает исключение redemption из DB-trigger и блокирует legacy redemption API/worker; применение к staging/production ещё не выполнялось.
- 2026-07-31: добавлена системная модель роста в `docs/OPEN_ABUNDANCE_SYSTEM_GROWTH_PLAN.md`: главная численная метрика — `Total Core = сумма Core всех участников`, а устойчивый подтверждённый прогресс, trust, wellbeing, retention и coverage являются условиями качества этого роста; AI развивается как координационный слой.
- 2026-07-31: текущий фокус System Growth Plan изменён: приоритет №1 — привлечение внешней ликвидности и рост Total Core; №2 — решение технических задач приложения, сайтов, интеграций, площадок, blockchain и AI силами участников за проверяемые Core-награды. Первые пользователи пилота ищут дополнительный заработок или готовы участвовать средствами в интересном проекте. Внутренние операции и системные Core-награды не зависят от нормы резервов; coverage ограничивает новые выводимые Wallet-обязательства. Минимально публичны `display_name`/псевдоним и `Level`, остальное управляется настройками приватности.

### Wallet

- Wallet — доступный внутренний баланс пользователя.
- Wallet используется для переводов, пользовательских заданий, внутренней торговли и будущего внешнего вывода.
- Wallet может получать не реферальный депозит, а долю ежедневного начисления от Core в соответствии с reinvest пользователя.
- В beta обучающие и первые системные челленджи не дают Wallet-награды.
- Командный процент и возможные поздние реферальные бонусы могут начисляться в Wallet только при достаточном покрытии.

### Вклад основателя и пользователей

- Начальный вклад основателя — ориентировочно `1000 USDT` с последующими пополнениями при необходимости.
- Это не заблокированный Wallet основателя. Средства вносятся через Wallet -> Core, после чего Wallet уменьшается, Core основателя увеличивается, а реальные активы остаются в Treasury.
- Аналогично пользователь может внести средства в Wallet и необратимо направить их в Core для ускорения роста.
- Нельзя считать устное обещание не выводить Wallet резервным механизмом; обязательство уменьшается только после фактической Wallet -> Core операции.

## 5. Резерв и покрытие

Основная внутренняя метрика:

```text
withdrawable_wallet_liability =
  сумма доступных Wallet
  + ожидающие выводы

coverage_ratio =
  подтвержденный баланс treasury-кошельков
  / withdrawable_wallet_liability
```

После Wallet -> Core соответствующая сумма перестает быть Wallet liability. Несколько treasury-кошельков агрегируются в один подтвержденный резерв, но имеют отдельные роли и адреса.

Пользователю beta не требуется показывать coverage ratio, runway или устройство Treasury. Ему нужны Core, Wallet, начисление за день, reinvest, история операций и понятный статус вывода. Финансовое здоровье системы показывается только на внутреннем операционном экране.

### Светофор ликвидности — продуктовая гипотеза

- **Зеленая зона:** покрытие не ниже установленного безопасного порога; вывод отправляется автоматически, только с минимальной сетевой/сервисной комиссией.
- **Желтая зона:** вывод либо идет в прозрачной очереди до одного часа, либо пользователь выбирает срочное исполнение с заранее известной повышенной комиссией.
- **Красная зона:** исполнение допускает очередь до суток либо более дорогой срочный маршрут; новые промо-, реферальные и другие Wallet-бонусы останавливаются.
- Уже начисленный Wallet нельзя задним числом блокировать или уменьшать.
- Точные пороги, комиссии и гарантированные сроки еще не определены и требуют финансовой и технической проверки.

Комиссия за срочность должна быть известна заранее и отражать реальную стоимость ликвидности. Она не должна превращаться в скрытый запрет вывода.

## 6. Treasury и вывод

Предпочтительная будущая структура:

- cold/multisig treasury для основного резерва;
- ограниченный hot payout wallet для автоматических выплат;
- отдельные escrow-кошельки торговли;
- отдельный учет полученных комиссий/выручки;
- автоматическое пополнение hot wallet по лимитам;
- emergency pause при расхождении ledger, атаке или нарушении заранее определенных условий.

Целевой UX — автоматический вывод без ручного ожидания, кроме времени исполнения и подтверждения транзакции USDT/TON. Перед автоматизацией нужны выбор сети, custody-модель, защита ключей, подтверждение адреса, лимиты, idempotency и обработка failed transactions.

Пилот ограничен закрытым allowlist пользователей. Первый внешний платежный срез — self-hosted пополнение Toncoin в TON с прямой проверкой on-chain finality и idempotent Wallet credit; USDT jetton добавляется после проверки native deposit pipeline. Юридические и KYC/AML-процессы не входят в технический scope этого пилота. Детали находятся в `docs/TON_DEPOSIT_MVP_PLAN.md`.

## 7. Действия при недостатке ликвидности

Рассматривались следующие направления:

- остановка новых Wallet-наград и акций при недостаточном покрытии;
- прозрачная очередь и заранее определенные режимы срочности;
- повышенные бонусы за добровольный Wallet -> Core;
- челленджи и streak за регулярный reinvest или неиспользование Wallet;
- привлечение дополнительной ликвидности;
- пополнение резерва фактической выручкой от торговли и сервисов;
- распределение резервов по нескольким кошелькам;
- отдельный emergency reserve.

Использование кредитных карт на grace period обсуждалось, но считается рискованным: это дорогой сроковый разрыв и не должно учитываться как резерв.

Стимулы за невывод должны быть добровольными. Вывод не должен отменять уже полученные уровни, сертификаты или ранее начисленные награды.

## 8. Сертификаты навыков

Статус 2026-08-01: нижеследующая экономика сертификатов сохранена как историческая гипотеза. Первый принятый слой заменён на Skill Passport с RPG-уровнями, evidence и практическими challenge; сертификаты и Wallet -> Core экономика сертификата отложены. Канонический план — `docs/SKILLS_SYSTEM_PLAN.md`.
Один из способов создавать полезность и снижать текущие Wallet liabilities — системные сертификаты навыков.

Пример направлений:

- привлечение и активация рефералов;
- производство контента;
- наставничество;
- продажи;
- проектная работа;
- подтвержденная помощь другим участникам.

Сертификат выдается после курса и проверяемого результата: рефералов, просмотров, выручки, завершенных задач или подтверждений участников.

Экономическая операция:

```text
Wallet пользователя уменьшается
-> та же сумма необратимо добавляется в Core
-> пользователь получает непередаваемый проверяемый сертификат
```

Сертификат не является буквально бесплатным: это добровольный reinvest ликвидного Wallet в Core. В интерфейсе операция должна называться честно. Сертификаты разных направлений и уровней могут быть требованиями для доступа к заданиям и челленджам.

Минимальные свойства сертификата: направление, уровень, критерии, доказательства, метод проверки, дата, issuer, verification record, публичная проверка и возможность revoke при обнаружении обмана.

## 9. Торговля и выручка

- Система может рекомендовать товары и услуги, подходящие для исполнения желаний пользователя.
- Продавец может платить системе процент от фактически завершенной продажи.
- Комиссия может включаться только после определенного порога оборота или суммы сделки.
- Возможна символическая комиссия за внутренние P2P/market операции, ориентировочно менее 1%, но точная модель не определена.
- Комиссия не является единственной anti-abuse защитой: нельзя награждать валовый оборот, нужны net-flow checks, уникальные контрагенты и выявление циклических переводов.
- В резерв засчитывается только фактически полученная выручка, а не прогноз GMV.
- Коммерчески мотивированные рекомендации должны быть маркированы; комиссия не должна автоматически покупать первое место в выдаче.

## 10. Идентичность и anti-abuse

- После ознакомительных челленджей системные награды предполагают доказательство уникального человека.
- Рассматриваются документы, биометрия и privacy-preserving proof-of-personhood наподобие World ID.
- Proof-of-personhood помогает против Sybil abuse, но не автоматически заменяет KYC/AML там, где они требуются.
- Для наград и переводов нужны non-self checks, idempotency, replay protection, связанные аккаунты/устройства, циклические переводы и ручной review подозрительных операций.

## 11. Текущее техническое состояние

В коде уже есть:

- guest identity, Google auth и referral claim;
- Wishes CRUD и рекомендации;
- Core/Wallet, уровни, daily accrual, reinvest, calculator и ledger;
- Wallet -> Core и Wallet-to-Wallet RPC/API/UI;
- Today technical MVP;
- challenges, autochecks и reward flow;
- local-first notes и tasks/streaks;
- feed/blog, public profiles, People, contacts и простой Direct;
- referrals/teams и team bonus foundation;
- Trust-lite;
- AI chat;
- Open Projects foundation;
- marketplace artifacts/listings и partial deal lifecycle с atomic completion; buyer funds escrow, expire/refund, dispute и User QA отсутствуют.

Большинство контуров технически реализованы, но не прошли связный User QA. Единственный существующий e2e smoke-тест устарел относительно гостевого onboarding.

Обновление 2026-07-12:

- удаленный Supabase-каталог подтвержден read-only проверкой: 17 активных челленджей;
- создан первый рекомендованный путь из пяти Core-челленджей без серверных prerequisites между шагами;
- 2026-07-13: `Turn On Core Growth` заменен на Today Core target challenge, потому что дефолтный `reinvest_percent > 0` не обучал пользователя действию; accepted-челленджи получили раскрываемый список и `Give up`;
- 2026-07-13: первая Core-цепочка принята пользователем и перенесена в `Подтверждено пользователями`; следующий активный шаг — единый onboarding;
- 2026-07-13: первый onboarding переориентирован с feed/referral showcase на `история -> главное желание -> финансовый план -> первый Core-путь`; на этой итерации сохраняется local draft и `profile.onboarding_state.firstPlanDraft`, но полноценные серверные Wish/GrowthPlan из draft остаются отдельной задачей;
- 2026-07-13: пользователь вручную прошел единый onboarding, открыл первый Core-путь и подтвердил сценарий; карточка перенесена в `Подтверждено пользователями`, следующая активная карточка — Home/Today как главный экран;
- 2026-07-13: для Home/Today принято решение использовать отдельный default `home` main tab, существующий no-store `/api/today` и onboarding draft как fallback; Home показывает ровно один CTA, а глобальный Today popup заменяется встроенным Home-состоянием. Полноценное server-side создание Wish/GrowthPlan из draft остается отдельной задачей;
- 2026-07-13: Home/Today технически реализован в `HomeTodayApp`, `AppNavigation`, `ChallengesApp`, `WalletApp` и локализации; остаются Technical QA/User QA, browser QA недоступен, а два Challenges smoke-сценария не получили каталог данных и остались на Loading;
- 2026-07-15: пользователь вручную подтвердил Home/Today и объединение Home с AI chat в одной навигационной группе; Home-карточка перенесена в `Подтверждено пользователями`, а стартовый маршрут позже возвращен на `Goals → Notes` из-за offline-first ценности Notes;
- 2026-07-15: пользователь подтвердил начисление challenge rewards в Core и MVP UX receipt в `ChallengesApp` (challenge, verification, сумма и Core после начисления); карточка перенесена в `Подтверждено пользователями`, а ledger/финансовая история и server-side изменение личного плана отложены до финансового этапа;
- 2026-07-15: первая версия Goals Growth Map реализована в `GrowthMapApp` и подключена во вкладку Goals; карта полезна как ориентация по уровням/Core/желаниям, но action-loop с Today/следующим challenge и динамикой плана оставлены для отдельного User QA/следующего этапа;
- 2026-07-15: активным шагом стал `Verified Reality Feed`; первый slice — server-backed `Challenge Done` без reward amount/ledger, с явным разделением verified system facts, `Демо` и ручного пользовательского контента;
- 2026-07-15: первый content slice Reality Feed переделан в server-backed модель и применен к Supabase: 23 fictional истории от первого лица стали обычными `feed_posts.post_type = reality_demo`, добавлены 46 RU/EN переводов и 23 внешних изображения через общую `feed_post_media`; `SocialApp` показывает их в общей ленте с маркировкой `Демо-история`, а real verified system cards остаются следующим серверным срезом;
- 2026-07-15: для следующей редакции demo-историй принят общий narrative direction: герой начинает из боли и ощущения отсутствия выхода, случайно узнает об Abundance, пробует из любопытства, проходит маленькие последовательные шаги и постепенно понимает, что способен менять свою жизнь; финал должен передавать простоту, свободу и реальный выбор без обещаний гарантированного дохода;
- 2026-07-19: migration `20260719123000_reality_feed_updated_demo_and_system_stories.sql` применена к Supabase: 10 согласованных demo-текстов обновлены по стабильным `source_key`, добавлены аккаунт `Abundance System`, 12 ordered `system_story`, 24 RU/EN-перевода и 12 Unsplash-изображений; `SocialApp` показывает системный аватар, отдельный бейдж и фото 4:5; отдельный профиль аккаунта и source pack для экономических тезисов остаются pending;
- 2026-07-20: отдельный профиль `Abundance System` реализован поверх feed API: имя/аватар и явная ссылка `Все главы` открывают 12 server-backed глав по порядку, возврат восстанавливает позицию ленты; мобильная лента ограничена шириной viewport и переносит длинный текст без горизонтального скролла; source pack для экономических тезисов остается pending;
- 2026-07-24: после изолированного Pinterest hotlink-теста migration `20260724181000_pinterest_reality_feed_media.sql` перевела все 35 изображений Reality Feed (`23 reality_demo + 12 system_story`) на разные проверенные `i.pinimg.com` URL; исходные Unsplash URL и attribution сохранены в metadata, а целевой переход на собственный S3/CDN зафиксирован в `FEED_POSTING_RECOMMENDATIONS_PLAN.md`;
- 2026-07-20: onboarding упрощен с пяти шагов до трех экранов `миссия -> истории -> программа 20 уровней`; формы и оговорки убраны из первого знакомства, добавлены три яркие иллюстрации, CTA расчета открывает гостевой Core-калькулятор с целью $1,000,000, а альтернативный CTA ведет к первому заданию;
- 2026-07-20: визуальный язык ярких 3D-иллюстраций, три референса и исходные prompts закреплены в `docs/VISUAL_LANGUAGE.md`; первый экран позиционирует Abundance как ИИ-инструмент создания изобилия и ускорения движения к желаниям;
- 2026-07-20: пятислойная модель `Безопасность -> Свобода -> Реализация -> Признание -> Единство` закреплена в `docs/WISH_PROGRESS_PYRAMID.md` как пирамида глубины желаний; рекомендуемое место — будущий необязательный блок `Goals -> Wishes`, отдельно от Core-уровней и без миграции до UI-прототипа;
- 2026-07-21: в `Goals -> Notes` реализован local-first Reflection Inbox: одно поле захвата без сетевого вызова, умный список `Разобрать`, guided AI-процесс `чувства -> возможные причины/потребности -> желаемое изменение -> доступное действие`, собственный вариант на каждом шаге, максимум два адаптивных уточнения, редактируемое я-высказывание и prefilled задача в Checks. Сырая запись не синхронизируется; push хранит только нейтральную delivery metadata и требует отдельной настройки VAPID/Vault.
- 2026-07-22: Reflection Inbox получил локальный `reviewAt` через 24 часа и синтетический агрегированный пункт на Home/Today без отправки текста или счётчика заметок в Supabase; отдельный inbox reminder заменён единым Today reminder. `/api/today` теперь отдаёт lifetime total и активную completion-серию, а каталог получил серверные проверки `today_completion_streak_7` и `today_completion_total_30`.
- 2026-07-22: принято направление единого AI-контекста: общий AI Gateway и системная база, отдельный function context/allowlist для каждой capability, consent scopes для публичных и private данных, Context Broker и только подтверждаемая пользователем долговременная память. Текущие chat, Home и Reflection контексты пока изолированы; подключение новых личных данных до реализации consent-контракта запрещено. Детали в `docs/AI_CONTEXT_MEMORY_ARCHITECTURE.md`.
- 2026-07-22: user-facing guest mode удален: третий экран onboarding теперь завершается обязательным Google-входом, unauthenticated shell больше не монтируется, а прежний `Save Your Progress` скрыт из каталога и сохранен только для idempotent выдачи стартового бонуса `+2$ Core`. После первого начисления показывается одноразовое окно награды и открывается лента; внутренний `guestIdentity` остается для referral/analytics и переноса существующих локальных данных.
- 2026-07-28: первый-session onboarding оформлен двумя обычными челленджами (`attention_value_audit`, `core_law_understood`) с серверным proof и `+1$ Core`; отдельный UI First Core Path удален, а daily unread точки используют локальный ключ user+дата. Старый технический контур защиты при невыплате и breach-only redemption теперь считается legacy; redemption запрещен базовым принципом неснижаемого Core.
- 2026-07-31: Home | Ideas chat и Reflection переведены на единую версионированную AI-базу знаний и общий Gemini/Groq gateway. Системный лор отделён от клиентского welcome-контента, а приватная reflection-запись передаётся как user input; Context Broker, consent scopes и долговременная память остаются отдельными этапами.
- добавлена минимальная first-party аналитика продуктовой воронки;
- smoke/e2e разделен на нового гостя с onboarding и возвращающегося гостя с app shell; оба сценария проходят;
- полная визуальная User QA цепочки остается обязательной.

## 12. Открытые вопросы

- Следующий актив/сеть после проверки Toncoin/TON и USDT/TON.
- Точные пороги зеленой, желтой и красной зон.
- Размер минимальной, обычной и срочной комиссии.
- Чем покрывается фиксированная ставка в долгом горизонте.
- Правила системной Core-эмиссии и оценка будущих Wallet liabilities.
- Критерии первого полезного челленджа и первых сертификатов.

## 13. Источники подробного контекста

- `docs/Abundance_SYS_CONCEPT` — исходная системная концепция.
- `docs/CHALLENGES_CATALOG.md` — каталог и правила челленджей.
- `docs/SKILLS_SYSTEM_PLAN.md` — Skill Passport, уровни, peer review и peer-points.
- `docs/WALLET_CRYPTO_RAILS_PLAN.md` — TON withdrawal, комиссии, custody и следующие asset/network rails.
- `docs/FUTURE_SIM_PLAN.md` — границы и этапы образа будущего.
- `docs/FEED_POSTING_RECOMMENDATIONS_PLAN.md` — user content, media и внешние соцсети.
- `docs/REFERRALS_TEAMS_PLAN.md` — рефералы, команды и бонусы.
- `docs/TRUST_RECIPROCITY_MARKET_PLAN.md` — Trust и взаимные подтверждения.
- `docs/MARKETPLACE_ESCROW_PLAN.md` — торговля, ownership и escrow roadmap.
- `docs/TODAY_DAILY_CHALLENGE_PLAN.md` — текущий Today MVP.
- `docs/PUBLIC_USER_DISCOVERY_PROFILE_PLAN.md` — поиск людей и публичные профили.
- `docs/BLOGGER_OUTREACH_PLAN.md` — предложение для блогеров и монетизация привода аудитории.
- `docs/MUTUAL_CREDIT_MARKET_PLAN.md` — mutual credit, внутренние продажи, продвижение по тратам.
- `docs/LEADER_GROWTH_PROGRAM.md` — программа роста лидеров, обучение, сертификация.
- `docs/ADHD_FRIENDLY_INTERFACE_PRINCIPLES.md` — универсальные принципы интерфейса и AI-коммуникации для снижения нагрузки на внимание и рабочую память.

## 14. Правило ведения memory

- Новая существенная гипотеза, аргумент, ограничение или нерешенный вопрос добавляется сюда.
- После принятия решения его краткая исполнимая формулировка переносится в мастер-план.
- Текущий статус никогда не ведется здесь — только в канбане.
- Устаревшая информация не удаляется молча: она помечается как отмененная или замененная новым решением.

## 15. Новые направления (2026-07-24)

### 15.1. Программа роста лидеров
Зафиксирована концепция пути от 0 до лидера-наставника в `docs/LEADER_GROWTH_PROGRAM.md`. Ключевые механики:
- Базовый уровень: 10% от роста команды (реализовано)
- Бонус за выращенных лидеров: +5 к capacity, бейдж "Наставник", 10% от бонуса нового лида (предлагается)
- Сертификация навыков по направлениям (предлагается)
- Обучающие челленджи: "Приведи первого", "Помоги новичку", "Стань лидером" (не реализовано)

### 15.2. Работа с блогерами
Зафиксировано предложение для блогеров в `docs/BLOGGER_OUTREACH_PLAN.md`. Основной стимул: реферальная ссылка → команда → 10% от роста Core участников. Ключевые недостающие элементы: панель команды (UI), личный чат лид→участник, задачи от лида, публичный профиль лида.

### 15.3. Mutual Credit / Внутренние продажи
Зафиксирована механика в `docs/MUTUAL_CREDIT_MARKET_PLAN.md`. Принцип: rolling `settled_spent - settled_earned` → мягкий capped boost в выдаче. Deal lifecycle и atomic completion появились технически, но funds reserve, expire/refund, dispute, reviews и User QA всё ещё блокируют включение алгоритма.

### 15.4. Открытые вопросы по новым направлениям
- Кто разрабатывает учебные материалы для программы лидеров?
- Нужен ли отдельный лендинг для блогеров или достаточно экрана в приложении?
- Точный размер комиссии системы на marketplace?
- Кап для mutual balance boost?
- Нужен ли dispute flow в MVP marketplace?
- Как часто показывать блогеру отчет по команде?

## 16. Интеграция новых направлений (2026-08-01)

Приняты и разнесены по каноническим документам:

- Skills: `software_creation` (vibecoding как первый метод), content, influence, referrals, mentoring и sales; Skill Level не выше Core Level; первая реализация — один L1 vertical slice и peer review, без сертификатов.
- Referrals: простой Invite challenge считает одну регистрацию после первого результата; `referral_acquisition` L1 использует две регистрации, следующие уровни — quality activation/retention.
- Trust v2: будущий публичный summary, starter event, рейтинг 0.0–5.0, sqrt amount factor, linear level pair-cap, 10% signed effect rater и calendar-year `×0.9`; запуск только shadow → private → public.
- Native TON withdrawal: два связанных поля USD/TON, `1%` сверху + network fee reserve, existing address только как ограниченный hot operating wallet; production custody не остаётся в `.env`.
- Rails: native TON complete loop → USDT Jetton в TON → одна следующая asset/network pair.
- Feed: first-party media через текущий Supabase Storage adapter, external media как links, затем like → flat comments → canonical repost → outbound share.
- Mutual credit: capped rolling `spent - earned` signal только по legitimate settled deals; GMV не приравнивается к внешней ликвидности.
- Future Sim: поздняя честно маркированная simulation с opt-in, а не финансовое обещание.
- Первый workflow: обязательный Day 0 заканчивается желанием, планом, daily goal и первым результатом; деньги и referral добровольны; привлечение когорты начинается сейчас партиями по 5–10.

Разрешённые конфликты и safety corrections:

- пользовательское желание использовать один TON address сохранено только для закрытого ограниченного hot-wallet MVP; основной reserve отделяется до существенного объёма;
- cancelled/zero order может влиять на Trust только после доказанного двустороннего взаимодействия, а не по одной заявке;
- Marketplace Phase 3/4 признан partial foundation: atomic completion есть, настоящего buyer funds escrow и expire/refund/dispute ещё нет;
- `X × 365 × N` является линейной иллюстрацией, не обещанием пожизненного начисления;
- публичный Trust является целевым решением, но текущий Trust-lite остаётся непубличным до калибровки v2.

Открытые decision gates: withdrawal limits/custody production, `peer_point` price/refund/initial reviewers, Trust scale/start/constants/caps, media limits/moderation SLA, exact first cohort and founder outreach capacity.